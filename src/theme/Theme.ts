import Colors from "./Colors";
import Fonts from "./Fonts";
import { Pixels, Size, isSmallDevice, window } from "./Layout";

export default {
  colors: Colors,
  fonts: Fonts,
  size: Size,
  window: window,
  isSmallDevice: isSmallDevice,
  pixels: Pixels,
};
