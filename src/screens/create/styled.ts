import { StyleSheet } from "react-native";
import { Size } from "../../theme/Layout";

export default StyleSheet.create({
  container: {
    width: "100%",
    height: "100%",
    gap: Size.s16,
  },
});
